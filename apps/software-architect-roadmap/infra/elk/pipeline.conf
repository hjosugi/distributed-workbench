input { tcp { port => 5000 codec => json_lines } }
filter { mutate { add_field => { "lab" => "architect-roadmap" } } }
output { elasticsearch { hosts => ["http://elasticsearch:9200"] index => "architect-logs-%{+YYYY.MM.dd}" } }
