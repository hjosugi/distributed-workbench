pipeline {
  agent any
  options { timeout(time: 10, unit: 'MINUTES') }
  stages {
    stage('Test') {
      steps {
        dir('apps/software-architect-roadmap') {
          sh 'python3 -m unittest discover -s tests -v'
          sh 'python3 -m architect_lab all'
          sh 'node --test examples/catalog/javascript/catalog.test.mjs'
        }
      }
    }
  }
}
